set nocompatible "modernize (disregard vi compatibility)

syntax on "turn syntax highlighting on

"enable plugins (for netrw)
filetype plugin on

"enable line numbers
set number

"show status bar
set laststatus=2
set statusline=%f "tail of the filename

